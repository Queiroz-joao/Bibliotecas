from datetime import datetime

# Obter a data e hora atuais
data_hora_atual = datetime.now()

# Exibir a data e hora de forma simples
print(f"Data e hora atuais: {data_hora_atual.strftime('%d/%m/%Y %H:%M:%S')}")
